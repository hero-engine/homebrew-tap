# hero-engine/homebrew-tap

Homebrew tap for [Hero](https://github.com/hero-engine/hero).

## Install

```bash
brew install hero-engine/tap/hero
```

Formula updates are committed automatically by CI when a new release is published.
